#!/usr/bin/env bash

set -euo pipefail
source "$BASE_DIR/framework.sh"

hostname=${HOSTNAME:-$(hostname)}

print_columns "" "${CA}$(figlet -w $WIDTH -c -p -f /usr/share/figlet/fonts/slant.flf $hostname)${CN}"
