alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
alias ~="cd ~"
alias l='ls'
alias ls='exa -aF --color=always --sort=type --group-directories-first --ignore-glob=".DS_Store" --icons' # lots of icons are causing lag in st
# alias ls='exa -aF --color=always --sort=type --group-directories-first --ignore-glob=".DS_Store"' 
# alias ls='lsd -A'
# alias ll='lsd -lA'
# alias lt='lsd --tree -A'
alias cp="cp -iv"
alias mv="mv -iv"
alias rm="rm -v"
alias bc="bc -ql"
alias mkdir="mkdir -pv"
alias grep="grep --color=auto"
alias diff="diff --color=auto"

alias find="fd"

# alias wn="wal -a 85 --backend haishoku -i ~/files/photos/wallpapers/wal/ && pywalfox update"
alias w="wal -a 85 --backend haishoku -i ~/files/photos/wallpapers/wal/ && pywalfox update"
alias wr="killall -USR1 st"
# alias w="wal -a 85 --backend haishoku -i ~/files/photos/wallpapers/wal/ && killall -USR1 st"
alias w1="wal -a 85 -i ~/files/photos/wallpapers/wal/ && pywalfox update && killall -USR1 st"
alias w2="wal -a 85 --backend colorz -i ~/files/photos/wallpapers/wal && pywalfox update && killall -USR1 st"
alias w3="wal -a 85 --backend colorthief -i ~/files/photos/wallpapers/wal/ && pywalfox update && killall -USR1 st"
alias wnew="wal -a 85 --backend colorz -i ~/files/photos/wallpapers/new/ && pywalfox update && killall -USR1 st"

# alias w="wal -a 90 -i ~/files/photos/wallpapers/wal && pywalfox update"
# alias w1="wal -a 90 --backend haishoku -i ~/files/photos/wallpapers/wal/"
# alias w2="wal -a 90 --backend colorz -i ~/files/photos/wallpapers/wal/"
# alias w3="wal -a 90 --backend colorthief -i ~/files/photos/wallpapers/wal/"
# alias wnew="wal -a 90 --backend colorz -i ~/files/photos/wallpapers/new/"

alias config="/usr/bin/git --git-dir=$HOME/.cfg/ --work-tree=$HOME"
alias cfg="config"

alias cr="cargo run"
alias crr="cargo run --release"
alias cb="cargo build"
alias cbr="cargo build --release"
alias cf="cargo fetch"
alias rr='./run'
alias rt='./test'
alias mr="make && ./run"
alias jj='nohup java -jar'
alias smci='sudo make clean install'

alias ka="killall"

alias python="python3"
alias calpoly="ssh ckfreedm@unix3.csc.calpoly.edu"
alias cpftp="sftp ckfreedm@unix3.csc.calpoly.edu"

alias top="htop"
alias pm="pulsemixer"
alias g="git"
alias lg="lazygit"
alias nf="neofetch"
alias vim="nvim"
alias v="nvim"
alias v="nvim"
alias e="$EDITOR"
alias t='tmux attach -t $(tmux ls | grep -v attached | head -1 | cut -f1 -d:) || tmux'
alias tm="tmux new-session -A -s main"
alias t0="tmux a -t 0 || tmux new"
alias t1="tmux a -t 1 || tmux new"
alias matrix="neo-matrix -d 0.5 -f 144 -G 0 -s -S 5.5 --maxdpc=2"
alias fontpreview='fontpreview-ueberzug'

alias wttr="curl wttr.in/San+Luis+Obispo"
alias wttr2="curl v2d.wttr.in/San+Luis+Obispo"


# calcurse
alias ccal="cal"
alias cal="calcurse"
alias c="calcurse"

# linux system
alias dual="exec /home/carson/bin/displays/dual.sh"
alias triple="exec /home/carson/bin/displays/triple.sh"
alias laptop="exec /home/carson/bin/displays/laptop.sh"
alias uw="exec /home/carson/bin/displays/ultrawide.sh && w"
alias ultrawide="exec /home/carson/bin/displays/ultrawide.sh"

alias pmrestart="systemctl --user restart pipewire.service pipewire.socket pipewire-pulse.socket"
alias pwrestart="systemctl --user restart pipewire.service pipewire.socket pipewire-pulse.socket"
alias battery='acpi'
alias ctrl='setxkbmap -option ctrl:nocaps'
alias bright="sudo bright"
alias autobright="sudo autobrightness"

alias egpu="sudo egpu-switcher"
alias gpu="sudo egpu-switcher"

# pacman aliases
alias pac='sudo pacman -S' # install
alias pacr='sudo pacman -Rs' # remove
alias pacs='pacman -Ss' # search
alias paci='pacman -Si' # info
alias paclo='pacman -Qdt' # list orphans
alias pacro='paclo && sudo pacman -Rns $(pacman -Qtdq)' # remove orphans
alias pacc='sudo pacman -Scc' # clean cache
alias paclf='pacman -Ql' # list files

